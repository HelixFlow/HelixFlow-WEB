import { 
  GET_API_LINK,
  PARSE_SCHEMA,
  GET_SCHEMA,
  TEST_TOOL,
  TOOL_CODE,
  TOOL_AUTH 
} from '@/constants/api'
import { request } from 'umi';

export async function getApiLink() {
  // return request(GET_API_LINK, {
  //   method: 'GET',
  // });
};
